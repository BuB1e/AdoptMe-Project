# AdoptMe-Project

## How to run
    1. Clone project
    2. Open Docker desktop
    3. run command
    ```
    docker-compose up -d --build
    ```
## If update backend
    - Run command
    ```
    docker-compose restart
    ```